{%- comment -%}
  Ce fichier documente le bloc JSON réutilisable à copier dans chaque {% schema %}.
  Il n'est pas rendu — il sert de référence copier-coller.

  USAGE : copier le contenu du tableau ci-dessous dans le "settings": [] de votre schema,
  juste avant votre premier { "type": "header" } de contenu.
{%- endcomment -%}
